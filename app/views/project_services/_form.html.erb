<%= form_for(@project_service) do |f| %>
  <% if @project_service.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@project_service.errors.count, "error") %> prohibited this project_service from being saved:</h2>

      <ul>
      <% @project_service.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
