<h1>Listing requirement_types</h1>

<table>
  <tr>
    <th>Name</th>
    <th>Abbreviation</th>
    <th>Description</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @requirement_types.each do |requirement_type| %>
  <tr>
    <td><%= requirement_type.name %></td>
    <td><%= requirement_type.abbreviation %></td>
    <td><%= requirement_type.description %></td>
    <td><%= link_to 'Show', requirement_type %></td>
    <td><%= link_to 'Edit', edit_requirement_type_path(requirement_type) %></td>
    <td><%= link_to 'Destroy', requirement_type, :confirm => 'Are you sure?', :method => :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Requirement type', new_requirement_type_path %>
